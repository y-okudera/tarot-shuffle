import TarotCard from "@/app/features/tarotCard";
import styles from "./page.module.css";

export default function Home() {
  return (
    <main className={styles.main}>
      <TarotCard />
    </main>
  );
}
